import { StyleSheet } from "react-native";

export const dialogStyles = StyleSheet.create({
  input: {
    marginBottom: 12,
  },
  rangeRow: {
    flexDirection: "row",
    alignItems: "center",
  },
  rangeInput: {
    flex: 1,
  },
  separator: {
    marginHorizontal: 12,
    fontSize: 20,
  },
});
